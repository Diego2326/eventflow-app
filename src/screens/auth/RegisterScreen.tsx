import { useState } from 'react'
import { Pressable, SafeAreaView, Text, TextInput } from 'react-native'
import { request } from '../../services/api'
import { styles } from '../../theme/styles'
export function RegisterScreen(){const[name,setName]=useState(''),[email,setEmail]=useState(''),[phone,setPhone]=useState(''),[password,setPassword]=useState(''),[message,setMessage]=useState('');async function submit(){try{await request('/auth/register','POST',{name,email,phone,password});setMessage('Cuenta creada. Revisa el enlace de activación.')}catch(error){setMessage((error as Error).message)}}return <SafeAreaView style={styles.auth}><Text style={styles.title}>Crea tu cuenta</Text><TextInput style={styles.input} placeholder="Nombre" value={name} onChangeText={setName}/><TextInput style={styles.input} placeholder="Correo" value={email} onChangeText={setEmail} autoCapitalize="none"/><TextInput style={styles.input} placeholder="Teléfono" value={phone} onChangeText={setPhone}/><TextInput style={styles.input} placeholder="Contraseña segura" value={password} onChangeText={setPassword} secureTextEntry/><Pressable style={styles.button} onPress={submit}><Text style={styles.buttonText}>Registrarme</Text></Pressable>{!!message&&<Text style={styles.notice}>{message}</Text>}</SafeAreaView>}
