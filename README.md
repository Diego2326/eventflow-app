# EventFlow App

Aplicación móvil construida con Expo y React Native. Renderiza la navegación del evento dinámicamente según los módulos habilitados.

```bash
cp .env.example .env
npm install
npm start
```

Configura `EXPO_PUBLIC_API_URL` con una URL accesible desde el dispositivo o emulador.

